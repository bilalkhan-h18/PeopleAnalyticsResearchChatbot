# corpus/

Put your PDFs here. Sub-folders are optional and become a "collection" label, e.g.

```
corpus/
  attrition/
  engagement/
  hr-analytics/
  methods/
```

PDFs are git-ignored. Only share papers you're licensed to share.
Scanned PDFs need OCR first (e.g. `ocrmypdf in.pdf out.pdf`).
